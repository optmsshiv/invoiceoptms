# ================================================================
#  OPTMS Invoice Manager — environment config
#  This file holds real secrets. Keep it OUTSIDE the web root if
#  possible, and NEVER commit it to git (see .gitignore).
#  Permissions: chmod 600 .env
# ================================================================

# ── Master DB ──────────────────────────────────────────────────
MASTER_DB_HOST=localhost
MASTER_DB_NAME=edrppymy_optms_master
MASTER_DB_USER=edrppymy_optms_master
MASTER_DB_PASS=123@optms_master
MASTER_DB_CHARSET=utf8mb4

# ── cPanel API (tenant DB provisioning) ───────────────────────
# Rotate this immediately if it was ever shared/pasted anywhere.
CPANEL_HOST=https://invcs.optms.co.in:2083
CPANEL_USERNAME=edrppymy
CPANEL_API_TOKEN=BMN3WI0RGV07SO1GEHZCSTSLWP337Y97

# ── App ────────────────────────────────────────────────────────
APP_NAME="OPTMS Tech Invoice Manager"
APP_VERSION=2.0.0
APP_URL=https://invcs.optms.co.in

SESSION_LIFETIME=7200
UPLOAD_MAX_SIZE=3145728
